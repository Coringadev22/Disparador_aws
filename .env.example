# ============================================
# DJANGO SETTINGS
# ============================================
# SECURITY WARNING: Keep the secret key secret in production!
# Generate a new one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-change-in-production

# SECURITY WARNING: Don't run with debug turned on in production!
DEBUG=True

# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# DATABASE
# ============================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://emailuser:emailpass123@db:5432/emailplatform

# ============================================
# REDIS & CELERY
# ============================================
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# ============================================
# AWS SES CONFIGURATION
# ============================================
# IMPORTANT: Get these credentials from AWS IAM Console
# 1. Go to: https://console.aws.amazon.com/iam/
# 2. Create a new user with AmazonSESFullAccess policy
# 3. Generate access keys
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS Region where your SES is configured
# Options: us-east-1, us-west-2, eu-west-1, etc.
AWS_SES_REGION=us-east-1

# Configuration Set name (optional but recommended)
# Create one in AWS SES Console > Configuration sets
AWS_SES_CONFIGURATION_SET=your-configuration-set-name

# ============================================
# SES RATE LIMITING
# ============================================
# Maximum emails per second (adjust based on your SES sending limits)
# Default SES sandbox: 1/second, Production: 14/second (tier dependent)
SES_RATE_LIMIT_PER_SECOND=14

# ============================================
# FRONTEND
# ============================================
# Backend API URL (change for production)
VITE_API_URL=http://localhost:8000

# ============================================
# NOTES FOR PRODUCTION
# ============================================
# Before deploying to production:
# 1. Change SECRET_KEY to a strong random value
# 2. Set DEBUG=False
# 3. Update ALLOWED_HOSTS with your domain
# 4. Use secure passwords for database
# 5. Enable HTTPS
# 6. Review SECURITY.md for complete checklist
